---
inclusion: manual
---

# Runtime TypeScript

Mantener tipado estricto, scripts `lint`, `format`, `test`, configuracion de `eslint` y `prettier`, y separacion clara entre runtime, infraestructura y pruebas.
